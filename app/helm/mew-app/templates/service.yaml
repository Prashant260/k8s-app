apiVersion: v1
kind: Service
metadata:
  name: {{ include "mew-app.fullname" . }}-service
  labels:
    {{- include "mew-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mew-app.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
